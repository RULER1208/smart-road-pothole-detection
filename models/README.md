# Model Weights

The trained model weights are not stored in the normal GitHub repository because they are large files.

Required model files:

- best_pothole_yolov8n.pt
- best_ssd300_vgg16_pothole.pth
- best_faster_rcnn_pothole.pth

For local use, place the downloaded model weights inside this folder.
